import { Dimensions } from "react-native";
const screenWidth = Dimensions.get("window").width;
export const font131517 = screenWidth < 400 ? 13 : screenWidth < 400 ? 15 : 17;
export const font1216 = screenWidth < 400 ? 12 : screenWidth < 600 ? 14 : 16;
export const font1820 = screenWidth < 400 ? 18 : screenWidth < 600 ? 20 : 22;
export const font2024 = screenWidth < 400 ? 20 : screenWidth < 600 ? 22 : 24;
export const font911 = screenWidth < 400 ? 9 : screenWidth < 600 ? 10 : 11;
export const font3640 = screenWidth < 400 ? 36 : screenWidth < 600 ? 38 : 40;
